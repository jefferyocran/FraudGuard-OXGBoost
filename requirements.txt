# Install with: pip install -r requirements.txt
# Versions left unpinned to stay compatible with Google Colab's current NumPy 2.x.
# To record the exact versions you used, run in a notebook cell:
#   !pip list | grep -E "xgboost|scikit-learn|numpy|pandas|shap"

numpy>=2.0
pandas>=2.0
scikit-learn>=1.4
xgboost>=2.1
shap>=0.46
